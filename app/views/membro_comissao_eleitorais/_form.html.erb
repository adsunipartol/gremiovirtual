<%= form_for(@membro_comissao_eleitoral) do |f| %>
  <% if @membro_comissao_eleitoral.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membro_comissao_eleitoral.errors.count, "error") %> prohibited this membro_comissao_eleitoral from being saved:</h2>

      <ul>
      <% @membro_comissao_eleitoral.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comissao_eleitoral_id %><br>
    <%= f.text_field :comissao_eleitoral_id %>
  </div>
  <div class="field">
    <%= f.label :aluno_id %><br>
    <%= f.text_field :aluno_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
