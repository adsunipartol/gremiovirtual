<%= form_for(@cargo_membro_chapa) do |f| %>
  <% if @cargo_membro_chapa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cargo_membro_chapa.errors.count, "error") %> prohibited this cargo_membro_chapa from being saved:</h2>

      <ul>
      <% @cargo_membro_chapa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao, 'Descrição' %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
